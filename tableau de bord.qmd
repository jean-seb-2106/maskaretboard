---
title: "Tableau de bord mensuel Maskaret"
author: "Equipe TICE"
format: 
  dashboard:
    theme: superhero
    logo: images/logo_cefil_new.jpg
    self-contained: true
---

```{r}
library(ggplot2)
```

```{r}
connexions <- read.csv(file = "data/connexions_uniques.csv",
                       sep = ";",
                       header = FALSE,
                       col.names = c("nombre_connexions","mois"))
connexions$mois <- as.Date(paste0(connexions$mois, "-01"), format = "%Y-%m-%d")



```

**Août 2025**

# Indicateurs {orientation="columns"}

## Connexions uniques {width="50%"}

### Row {height="30%"}

```{r}
#| content: valuebox
#| title: "Connexions uniques"
#| icon: plug
#| color: primary
84
```

### Row {height="70%"}

```{r}
#| fig-width: 14
#| fig-height: 8
#| title: "Evolution mensuelle du nombre de connexions uniques"
ggplot(connexions, aes(x = mois, y = nombre_connexions)) +
  geom_line(color = "blue", size = 1) +  # Ligne bleue
  geom_point(color = "red", size = 2) +   # Points rouges
  labs(
    title = "Évolution du nombre de connexions uniques",
    x = "Mois",
    y = "Nombre de connexions"
  ) +
  theme_minimal() +  # Thème épuré
  scale_x_date(date_labels = "%b %Y", date_breaks = "1 month")  # Format des dates
```

## Stats sur les e-formations {width="50%"}

### Row {height="30%"}

```{r}
#| content: valuebox
#| title: "e-formation disponibles"
#| icon: pencil
#| color: success
38
```

```{r}
#| content: valuebox
#| title: "Badges délivrés"
#| icon: trophy
#| color: warning
38
```

### Row {height="70%"}

```{r}
#| title: "TOP 10 des cours les plus suivis"
knitr::kable(mtcars)
```

# Lexique

## Connexions uniques

-   [N**ombre de connexions uniques** :]{.underline} correspond au **nombre distinct d'utilisateurs** qui se sont connectés au moins une fois à Maskaret pendant le mois en cours.

## Formations disponibles

-   [**Formations disponibles** :]{.underline} ce sont les formations hébergées sur MasKaret, auxquelles les apprenants peuvent s'inscrire. Ils peuvent s'y inscrire soit en **auto-inscription** soit en **inscription manuelle** (par l'administrateur de la plateforme). Les espaces de ressources pour les stagiaires du Cefil ne sont pas comptabilisés comme des formations.

## Badges

-   [Un **badge**]{.underline} est délivré aux apprenants quand ils ont achevé toutes les activités d'un cours (une e-formation). Il faut notamment qu'ils aient obtenu au moins 70% de bonnes réponses aux quiz de ce cours.
